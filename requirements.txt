# UIFPI — Python dependencies
# Install with: pip install -r requirements.txt
# Then: playwright install chromium

# Core scraping
playwright>=1.40.0
beautifulsoup4>=4.12.0
requests>=2.31.0

# Database (stdlib sqlite3 is sufficient — no extra package needed)

# Claude API + env management
anthropic>=0.34.0
python-dotenv>=1.0.0

# Data analysis
pandas>=2.1.0
numpy>=1.24.0
scipy>=1.11.0
statsmodels>=0.14.0
